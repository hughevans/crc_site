%h1 Listing topics

%table
  %tr
    %th Title
    %th Description
    %th Link
    %th Meeting

  - for topic in @topics
    %tr
      %td=h topic.title
      %td=h topic.description
      %td=h topic.link
      %td=h topic.meeting_id
      %td= link_to 'Show', topic
      %td= link_to 'Edit', edit_topic_path(topic)
      %td= link_to 'Destroy', topic, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New topic', new_topic_path
